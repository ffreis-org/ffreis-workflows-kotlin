package hello

/** Minimal example subject exercised by the kotlin-*.yml self-test workflows. */
object Calculator {
    fun add(a: Int, b: Int): Int = a + b

    fun multiply(a: Int, b: Int): Int = a * b
}
